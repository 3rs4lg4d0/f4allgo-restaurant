// Package service includes DDD applications services that implements domain
// operations dealing directly with domain objects. We should put in
// application services the domain operations that don't naturally fit into
// any particular aggregate root.
package service
